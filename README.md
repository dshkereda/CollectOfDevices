# 🧭 Инструкция по запуску краулера **AllPribors Crawler**

Эта инструкция поможет вам шаг за шагом **собрать данные с сайта [https://all-pribors.ru](https://all-pribors.ru)** по заданному регистрационному номеру и датам поверки.

Просто вставляй все команды в терминал и нажимай enter

---

## 📝 1. Что делает скрипт

Скрипт автоматически:
- открывает сайт и обходит все страницы результатов;
- кликает по карточкам приборов;
- извлекает таблицы с данными;
- сохраняет их в CSV-файл;
- может продолжать с места остановки при перезапуске;
- поддерживает обход по конкретной дате или диапазону дат поверки.

---

## 🌱 2. Начало работы и включение окружениея

1. Создайте папку где будет хранится скрипт и создаваемые файлы и поместите в неё файл requirements.txt, parser.py и converter.py

2. Создайте окружение:
   ```bash
   python3 -m venv venv
   ```

3. Активируйте окружение:
     ```bash
     source venv/bin/activate
     ```

4. Установите необходимые библиотеки:
   ```bash
   pip install -r requirements.txt
   ```


✅ После этого окружение готово — создать и установить окружение нужно один раз, потом его только активировать (шаг 3).

---

## 🚀 3. Как запускать краулер

Скрипт вызывается так:
```bash
python3 parser.py --rn РЕГНОМЕР [дополнительные параметры]
```

Параметры:

| Параметр             | Описание |
|----------------------|----------|
| `--rn`              | **Обязательный.** Регистрационный номер поверки, например `85773-22`. |
| `--date`            | Одна конкретная дата поверки (в формате `ГГГГ-ММ-ДД`). |
| `--date-range`      | Диапазон дат, например `2025-09-01:2025-09-17`. |
| `--out`            | Путь к CSV-файлу для сохранения результатов. По умолчанию `output.csv`. |
| `--headless`            | По умолчанию скрипт будет выполняться без открывания браузера, но можно выставлять False и тогда ты будешь видеть как выполняется краулинг. |

---

### 📅 Примеры

**1. Сбор данных за один день**
```bash
python3 parser.py --rn 85773-22 --date 2025-09-17 --out results.csv
```

**2. Сбор данных за диапазон дат**
```bash
python3 parser.py --rn 85773-22 --date-range 2025-09-01:2025-09-17 --out results.csv
```

**3. Сбор всех доступных данных**
```bash
python3 parser.py --rn 85773-22 --out results.csv
```

**4. Сбор всех доступных данных**
```bash
python3 parser.py --rn 91851-24 --headless False --out data.csv
```

---

## 📂 5. Результаты работы

После успешного запуска в папке появятся три файла:

| Файл                      | Назначение |
|---------------------------|-----------|
| `results.csv`            | Основной файл с выгруженными данными. |
| `results.progress.json`  | Промежуточные данные — запоминает, на какой странице и дате остановился краулер. |
| `crawler.log`            | Подробный журнал выполнения (для диагностики ошибок). |

📌 Пример `results.csv` (откроется в Excel или Google Таблицах):

| Наименование | Заводской № | Дата поверки | Организация | ... |
|-------------|------------|--------------|------------|-----|
| Термометр   | 12345      | 2025-09-17   | ФБУ "Ростест" | ... |
| Весы       | 67890      | 2025-09-17   | ООО "Метрология+" | ... |

---

## 🧭 6. Возобновление работы после сбоя

Если интернет оборвался или сайт завис:
- Ничего страшного ✅  
- Просто **запустите ту же команду снова**.  
- Краулер автоматически прочитает `progress.json` и продолжит с нужной даты и страницы.  

👉 Уже собранные данные не перезаписываются, а пропускаются.

---

## ⚡ 7. Советы по стабильной работе

- Не запускайте несколько копий краулера одновременно на один и тот же CSV — это может повредить файл.  
- Если сайт часто подвисает, можно **временно увеличить задержки** или включить перезапуск драйвера каждые 5 страниц (уже встроено).  
- Если Chrome зависает намертво — просто закройте окно терминала, подождите 1–2 минуты и запустите скрипт снова.

---


## ✅ 9. Краткое напоминание (шпаргалка)

```bash
# активация окружения
source venv/bin/activate        # macOS / Linux

# установка библиотек
pip install -r requirements.txt

# запуск на один день
python3 all_pribors_crawler.py --rn 85773-22 --date 2025-09-17 --out results.csv

# запуск на диапазон
python3 all_pribors_crawler.py --rn 85773-22 --date-range 2025-09-01:2025-09-17 --out results.csv

# продолжение после сбоя
(просто запусти ту же команду ещё раз)
```

## ✅ 10. Конвертация итогового файла в Excel

В конце необходимо конвертировать файл csv в xlsx, делается это с помощью скрипта converter.py

Скрипт выполняется так:

```bash
python3 converter.py input.csv [output.xlsx]
```
где input.csv - созданный ранее файл с данными, а output.xlsx - получаемый файл excel

### 📅 Пример

```bash
python3 converter.py data.csv data.xlsx
```

```bash
python3 converter.py RGUNG.csv university.xlsx
```

То есть мы сами задаем название конечного файла
